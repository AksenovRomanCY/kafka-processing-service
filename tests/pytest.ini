[pytest]
minversion = 6.0
addopts = -ra -q
testpaths = tests/unit
python_files = test_*.py

markers =
    asyncio: mark async tests

# Явно фиксируем область действия event_loop в function (по будущему поведению pytest-asyncio)
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
